# A project README, for scale

This file mimics a typical project `README.md`, the most common thing
`emdee-eyes` gets pointed at.

## Setup

```sh
git clone git@example.com:org/project.git
cd project
npm install
```

## Usage

Run `npm start`, then open <http://localhost:3000>.

## Configuration

| Variable       | Default | Purpose                  |
| -------------- | ------- | ------------------------- |
| `PORT`         | `3000`  | HTTP port to listen on    |
| `LOG_LEVEL`    | `info`  | `debug`, `info`, or `warn` |

## Contributing

1. Fork the repo
2. Create a branch: `git checkout -b my-feature`
3. Open a pull request

See [CONTRIBUTING.md](https://example.com/CONTRIBUTING.md) for the full
guide.
